# fake_type

An interactive fake typing manuscript page. Random keyboard input gradually reveals a formatted academic manuscript, supports imported PDF text, bilingual UI, typing audio, and an optional kinetic word-storm visual effect.

Open `index.html` directly, or publish the repository with GitHub Pages from the `main` branch root.
